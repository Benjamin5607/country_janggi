Rigged army GLBs (PNG → build-units)
====================================

  npm run build-units

Each file: soldier rig + Idle/Walk + nation PNG (background removed) baked on mesh.
Output: {region}_{team}.glb

Game loads these automatically. Do not use public/models/units/ (old extrusion).
