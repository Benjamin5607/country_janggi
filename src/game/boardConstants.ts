/** One intersection spacing on the janggi board (world units). */
export const BOARD_CELL = 1;

export const BOARD_FILES = 9;
export const BOARD_RANKS = 10;

/** Height of each checker tile (world Y). */
export const BOARD_TILE_HEIGHT = 0.09;

/** Y where unit feet stand — top surface of checker tiles. */
export const BOARD_PIECE_Y = BOARD_TILE_HEIGHT + 0.01;
